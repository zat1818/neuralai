export interface TickerItemProps {
  category: string;
  text: string;
}

export interface NewsItemProps {
  category: string;
  title: string;
  time: string;
  imageUrl?: string;
  description?: string;
}
